# coordinator.conf - basic test configuration

# Listen on all interfaces, default MQTT port 1883
listener 1883 0.0.0.0
socket_domain ipv4

# Allow anonymous clients to connect
allow_anonymous true

# Logging settings
log_type all
connection_messages true
log_timestamp true

# Persistence (off for simple testing)
persistence false
